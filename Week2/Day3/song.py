class Song:

    def __init__(self, name, artist, album, raiting, length, bitrate):
        self.name = name
        self.artist = artist
        self.album = album
        self.raiting = raiting
        self.length = length
        self.bitrate = bitrate
